# vim: ft=fish
if [ -d $HOME/.rbenv/shims -a -d $HOME/.cargo/bin ]
  set PATH $HOME/.rbenv/shims $HOME/.cargo/bin $PATH
  rbenv rehash >/dev/null ^&1
end

if test $XDG_SESSION_TYPE = "wayland"
    set -x MOZ_ENABLE_WAYLAND 1
end
